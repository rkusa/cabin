[package]
name = "crabweb-macros"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = ["derive"] }
quote = { version = "1.0" }
convert_case = "0.6"
